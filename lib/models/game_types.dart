// Define different game object types
enum GameObjectType {
  mouse,
  laserDot,
  bug,
  feather,
  yarnBall,
}

// Define different sound types for each game object
enum GameObjectSound {
  mouse,
  bug,
  laser,
  feather,
  yarnBall,
}
